#include "main.h"
#include <unistd.h>
/**
 * Function - prints the alphabet in lower case
 * Returns : void (Done)
 */
void print_alphabet(void)
{
	char i;

	for (i = 0; i <= 'z' ; i++
	{
		_putchar(i);
	}
}
